/* ============================================================
   extras.js — 커널 속으로: 전공노트·사실·퀴즈 (2편 콘텐츠팩)
   ============================================================ */

const EXTRAS = {
  process: {
    deep: [
      '프로세스 = <b>실행 중인 프로그램의 인스턴스</b>. 프로그램(디스크의 파일)과 프로세스(살아있는 실행체)의 구분이 OS 공부의 첫 어휘입니다.',
      '커널은 프로세스마다 <b>PCB</b>(Process Control Block)를 둡니다 — PID, 상태, 레지스터 사본, 주소 공간 포인터, 열린 파일 목록이 담긴 신분 서류철.',
      '프로세스의 상태 기계: <b>실행(Running) ↔ 준비(Ready) ↔ 대기(Blocked)</b> — I/O를 기다리면 대기로, 끝나면 준비로, 차례가 오면 실행으로.',
    ],
    facts: ['지금 이 순간 여러분의 컴퓨터엔 수백 개의 프로세스가 살아 있습니다 — 작업 관리자(top)로 직접 셀 수 있어요.'],
    quiz: { q: '프로그램과 프로세스의 차이는?', opts: ['프로세스는 실행 중인 프로그램의 인스턴스', '같은 말이다', '프로그램이 더 작다'], a: 0, why: '디스크의 레시피(프로그램)와 지금 끓고 있는 요리(프로세스) — 한 레시피로 여러 요리를 동시에 끓일 수도 있죠.' },
  },
  addrspace: {
    deep: [
      '각 구역은 권한이 다릅니다: 코드는 <b>읽기+실행</b>, 데이터·힙·스택은 <b>읽기+쓰기(실행 금지)</b> — 스택에 심은 공격 코드가 실행되지 않게 막는 보안 장치(NX 비트)입니다.',
      '이 지도의 실체가 1편에서 본 <b>페이지 테이블</b>입니다 — 구역별 권한 비트가 페이지 단위로 새겨져 있습니다.',
    ],
    facts: ['주소 공간 배치를 매번 무작위로 섞는 <b>ASLR</b> — 공격자가 주소를 못 찍게 하는 현대 OS의 기본기입니다.'],
    quiz: { q: '재귀 호출이 너무 깊어지면 넘치는 곳은?', opts: ['스택', '힙', '코드 영역'], a: 0, why: '함수 호출마다 스택 프레임이 쌓입니다 — 한계를 넘으면 스택 오버플로로 프로세스가 죽습니다.' },
  },
  threads: {
    deep: [
      '프로세스 생성은 무겁고(주소 공간 통째) 스레드 생성은 가볍습니다(스택 하나) — "동시에 여러 일"이 필요하면 보통 스레드부터 씁니다.',
      '<b>경쟁 상태</b>: count++조차 읽기-수정-쓰기 3단계라, 두 스레드가 겹치면 증가가 사라질 수 있습니다 — 락으로 임계 구역을 보호해야 합니다.',
      '락의 부작용이 <b>데드락</b>: 서로가 서로의 락을 기다리며 영원히 멈춤 — v2에서 깊이 다룹니다.',
    ],
    facts: ['파이썬의 GIL, 자바스크립트의 단일 스레드 이벤트 루프 — 언어 설계조차 이 동시성 문제와의 타협입니다.'],
    quiz: { q: '한 프로세스의 스레드들이 공유하지 않는 것은?', opts: ['자기 스택과 레지스터', '힙', '전역 변수'], a: 0, why: '실행 위치(레지스터·스택)만 따로 — 그래야 서로 다른 곳을 달릴 수 있으니까요.' },
  },
  syscall: {
    deep: [
      '시스템 콜은 <b>유일한 합법적 입구</b>입니다 — 커널 메모리를 직접 읽으려 하면 페이지 테이블의 권한 비트가 막고 segfault가 납니다.',
      '함수 호출보다 훨씬 비쌉니다(모드 전환 + 캐시 오염) — 그래서 라이브러리는 버퍼링으로 호출 횟수를 줄입니다. printf가 즉시 화면에 안 나올 때가 있는 이유!',
      '리눅스의 시스템 콜은 약 <b>300여 개</b> — 이 목록이 사실상 "커널이 제공하는 서비스의 전부"입니다.',
    ],
    facts: ['strace 명령으로 아무 프로그램의 시스템 콜을 실시간 구경할 수 있습니다 — ls 한 번에 수십 개가 쏟아져요.'],
    quiz: { q: '프로세스가 파일을 읽는 유일한 방법은?', opts: ['시스템 콜로 커널에 요청', '디스크에 직접 접근', '드라이버를 직접 호출'], a: 0, why: '하드웨어 접근 권한은 커널만 — 문은 하나뿐이고, 그래서 보안과 질서가 가능해집니다.' },
  },
  trap: {
    deep: [
      '트랩(의도된 진입)·인터럽트(외부 사건)·예외(명령의 사고)는 모두 같은 메커니즘을 탑니다 — <b>정해진 표를 통한 커널 진입</b>.',
      '모드 전환 비용 수백 사이클 + 캐시·TLB 오염 — 고성능 서버가 시스템 콜 횟수를 줄이려 io_uring 같은 배치 인터페이스를 쓰는 이유입니다.',
      '멜트다운(2018) 사태 이후 커널/유저 페이지 테이블을 아예 분리(KPTI)하면서 전환 비용이 더 비싸졌습니다 — 보안과 성능의 영원한 줄다리기.',
    ],
    facts: ['"trap"이라는 이름 그대로 — 프로세스는 함정에 빠지듯 커널로 떨어집니다. 자발적 함정이죠.'],
    quiz: { q: 'trap 명령이 아무 주소로나 점프할 수 없는 이유는?', opts: ['커널이 정한 입구(표)로만 진입 가능해서', 'CPU가 느려서', '주소를 몰라서'], a: 0, why: '입구를 커널이 통제하니까 유저 코드가 커널을 마음대로 주무를 수 없습니다 — 보호의 핵심.' },
  },
  kernel: {
    deep: [
      '설계 스펙트럼: 모든 것을 한 덩어리로(<b>모놀리식</b>, 리눅스) vs 최소만 커널에 두고 나머지는 유저 공간 서버로(<b>마이크로커널</b>, minix·QNX) — 성능 vs 격리의 트레이드오프.',
      '커널도 결국 프로그램입니다 — C로 짜이고, 버그가 나면 <b>커널 패닉</b>(블루스크린)으로 시스템 전체가 죽습니다. 그래서 커널 코드 리뷰는 혹독합니다.',
    ],
    facts: ['리눅스 커널은 약 3천만 줄 — 인류가 함께 짓는 가장 큰 건축물 중 하나입니다.'],
    quiz: { q: '커널이 답하는 근본 질문은?', opts: ['한정된 자원을 여럿이 어떻게 나눠 쓰나', '화면을 어떻게 예쁘게 그리나', '코드를 어떻게 컴파일하나'], a: 0, why: 'CPU 시간도 메모리도 디스크도 — 전부 "나눠 쓰기" 문제입니다. 커널의 방들이 전부 그 답이죠.' },
  },
  sched: {
    deep: [
      '스케줄링의 영원한 긴장: <b>처리량 vs 응답성 vs 공정성</b> — 셋을 동시에 최대화할 수 없습니다.',
      '리눅스 CFS는 프로세스마다 "지금까지 받은 CPU 시간"을 적어두고 <b>가장 적게 받은 쪽</b>을 고릅니다 — 빨강-검정 트리로 O(log n)에.',
      'I/O 많은 프로세스(에디터)는 짧게 자주, CPU 많은 프로세스(인코딩)는 길게 가끔 — 좋은 스케줄러는 이 둘을 자동으로 구분합니다.',
    ],
    facts: ['타임슬라이스는 보통 수 ms — 사람의 지각(~100ms)보다 훨씬 짧아 "동시"로 느껴집니다.'],
    quiz: { q: '한 프로세스가 무한루프에 빠져도 시스템이 안 멈추는 이유는?', opts: ['타이머 인터럽트가 강제로 스케줄러를 깨워서', 'CPU가 알아서 피해서', '무한루프는 금지라서'], a: 0, why: '수 ms마다 울리는 타이머 벨 — 어떤 프로세스도 CPU를 독점할 수 없습니다. 선점형 멀티태스킹의 핵심.' },
  },
  ctxswitch: {
    deep: [
      '직접 비용(레지스터 저장·복원, ~µs)보다 <b>간접 비용</b>이 큽니다: 캐시·TLB·분기 예측기가 전부 식어, 복귀 후 한동안 느리게 돕니다.',
      '같은 프로세스의 <b>스레드 간 전환은 더 쌉니다</b> — 주소 공간(지도)을 안 바꿔도 되니 TLB가 살아남습니다.',
      '그래서 서버 설계가 "스레드 풀", "이벤트 루프"로 진화했습니다 — 전환 자체를 줄이는 방향으로.',
    ],
    facts: ['초당 컨텍스트 스위치 횟수는 vmstat으로 볼 수 있습니다 — 평범한 데스크톱도 초당 수천 번.'],
    quiz: { q: '컨텍스트 스위치의 가장 큰 숨은 비용은?', opts: ['캐시·TLB가 식는 것', '레지스터 저장 자체', '화면 깜빡임'], a: 0, why: '저장·복원은 µs지만, 식은 캐시로 달리는 한동안의 느려짐이 진짜 청구서입니다.' },
  },
  memmgr: {
    deep: [
      '<b>요구 페이징</b>: 프로그램을 시작할 때 다 안 읽습니다 — 실제로 건드린 페이지만 그때그때 적재. 큰 프로그램이 빨리 뜨는 비결.',
      '교체 정책의 성배는 "미래에 가장 늦게 쓰일 페이지"(Belady 최적)지만 미래는 모르므로 — 과거(LRU)로 근사하고, LRU도 비싸서 Clock으로 또 근사합니다.',
      '<b>스래싱</b>: 워킹셋 > RAM이면 페이지 폴트의 폭주로 일은 못 하고 디스크만 바쁜 상태 — 해법은 결국 "덜 돌리거나 RAM을 늘리거나".',
    ],
    facts: ['1편 가상 메모리 노드의 미니랩(페이지 폴트 체험)이 바로 이 방의 하드웨어 쪽 풍경입니다.'],
    quiz: { q: 'fork()가 빠른 비결인 CoW의 동작은?', opts: ['지도만 복사하고 쓸 때만 진짜 복사', '메모리를 통째로 즉시 복사', '아무것도 복사 안 함'], a: 0, why: '대부분의 fork는 곧 exec로 이어지므로 — 통째 복사는 낭비였을 겁니다. 게으름이 미덕인 사례.' },
  },
  vfs: {
    deep: [
      '<b>VFS</b>(가상 파일시스템)는 인터페이스 층입니다 — ext4든 NTFS든 USB든 같은 open/read/write로 보이게 하는 어댑터.',
      '쓰기는 보통 <b>페이지 캐시</b>에 먼저 — 디스크엔 나중에(write-back). 정전 대비로 일지를 먼저 쓰는 게 <b>저널링</b>입니다(1편 저장장치 교재와 연결).',
      '디렉터리도 파일입니다 — "이름 → inode 번호" 표가 담긴 특별한 파일.',
    ],
    facts: ['"rm으로 지워도 데이터는 남는다" — 지우기는 inode 연결을 끊을 뿐, 블록 내용은 덮어쓸 때까지 그대로입니다.'],
    quiz: { q: 'open()이 프로세스에게 돌려주는 것은?', opts: ['파일 디스크립터(번호표)', '파일 내용 전체', '디스크 주소'], a: 0, why: '이후 read(3, …)처럼 번호로 대화합니다 — 내용은 read가 그때그때 가져옵니다.' },
  },
  driver: {
    deep: [
      '드라이버는 커널 모드에서 돕니다 — 그래서 <b>드라이버 버그 = 시스템 전체 사망</b>. 블루스크린의 단골 원인이 드라이버인 이유.',
      '대화 방식 둘: 바쁘게 묻기(<b>폴링</b>) vs 벨 기다리기(<b>인터럽트</b>) — 1편 제어 버스에서 본 그 선택지의 소프트웨어 쪽입니다.',
      '안정성을 위해 드라이버를 유저 공간으로 빼는 흐름도 있습니다(libusb, DPDK, 마이크로커널) — 죽어도 그 드라이버만 죽게.',
    ],
    facts: ['리눅스 커널 코드의 절반 이상이 드라이버입니다 — 세상의 하드웨어가 그만큼 다양하다는 뜻.'],
    quiz: { q: '새 장치를 사면 드라이버를 설치하는 이유는?', opts: ['커널은 그대로 두고 통역사만 추가하는 구조라서', '커널을 새로 사야 해서', '장치가 드라이버를 좋아해서'], a: 0, why: '일반 요청 ↔ 장치 고유 언어 사이의 번역 모듈만 끼우면 — 커널 본체는 수만 가지 장치를 몰라도 됩니다.' },
  },
  irqhandler: {
    deep: [
      '핸들러는 <b>상반부/하반부</b>로 나뉩니다: 상반부는 인터럽트 끈 채 최소한만(µs), 하반부(softirq·워크큐)는 나중에 여유롭게 — "벨 응대는 짧게, 뒷정리는 따로".',
      '핸들러 안에서는 잠들 수 없습니다(블로킹 금지) — 지금 멈춘 프로세스의 문맥을 빌려 도는 중이라, 잠들면 엉뚱한 피해자가 생깁니다.',
      '같은 인터럽트의 폭주를 막는 기법들(병합·폴링 전환)은 1편 NIC 노트에서 본 NAPI와 같은 문제의식입니다.',
    ],
    facts: ['키 입력·패킷 도착·디스크 완료·타이머 — 여러분이 컴퓨터를 쓰는 동안 인터럽트는 초당 수천 번 울리고 있습니다.'],
    quiz: { q: '인터럽트 핸들러의 철칙은?', opts: ['짧게 — 급한 최소한만 하고 나머지는 미룬다', '길게 — 한 번에 다 처리한다', '무시한다'], a: 0, why: '핸들러가 도는 동안 다른 벨이 밀립니다 — 상반부/하반부 분리가 그래서 존재합니다.' },
  },
  hwboundary: {
    deep: [
      '커널이 하드웨어를 만지는 통로는 결국 셋입니다: <b>MMIO</b>(장치 레지스터에 읽고 쓰기), <b>특권 명령</b>(페이지 테이블 교체·인터럽트 마스크), 그리고 하드웨어가 올려보내는 <b>인터럽트/예외</b>.',
      '이 경계의 약속이 1편에서 본 <b>ISA</b>입니다 — 커널도 결국 그 명령어들로 쓰인 프로그램일 뿐입니다.',
    ],
    facts: ['가상머신은 이 경계를 소프트웨어로 흉내 낸 것입니다 — 커널마저 속이는 또 한 겹의 환상(하이퍼바이저).'],
    quiz: { q: '커널과 하드웨어가 만나는 통로가 아닌 것은?', opts: ['HTML 렌더링', 'MMIO', '인터럽트'], a: 0, why: 'HTML은 저 위 유저 공간 브라우저의 일 — 경계의 언어는 레지스터·특권 명령·인터럽트입니다.' },
  },
};

window.EXTRAS = EXTRAS;
