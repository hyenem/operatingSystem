# 커널 속으로 — Descent into the Kernel (시리즈 2편)

> 더블클릭 한 번이 만든 **실행 중인 프로세스**에서 출발해, 시스템 콜의 문을 지나
> 커널의 방들을 거쳐 **하드웨어 경계**(=1편의 입구)까지 내려가는 운영체제 입문서.
> 시리즈 큰 그림: `../README.md`

## 줌 은유 — 권한/추상화 레벨
물리적 크기 대신 **추상화의 깊이**로 내려간다. 게이지는 권한 링:
`유저 공간(Ring 3) → 시스템 콜 경계 → 커널 공간(Ring 0) → 하드웨어 경계`

## 탐험 트리 v1 (굵은 경로 우선, 13노드)

```
D0  🖱 실행 중인 프로세스 (루트)                 [유저 공간]
├─ D1 주소 공간 (코드·데이터·힙·스택 지도)
├─ D1 스레드 (한 프로세스 안의 여러 실)
└─ D1 시스템 콜 — 커널로 들어가는 유일한 문      🧪 strace 여정
    └─ D2 모드 전환 (trap · Ring 3→0)           [경계]
        └─ D3 커널 (자원의 관리자, 플로어플랜)    [커널 공간]
            ├─ D4 스케줄러 — 누가 CPU를 쓸 차례인가
            │   └─ D5 컨텍스트 스위치 — 우주를 통째로 갈아끼우기
            ├─ D4 메모리 관리자 — 교체·스왑·CoW (1편 가상메모리의 나머지 절반)
            ├─ D4 파일시스템(VFS) — "모든 것은 파일"
            └─ D4 디바이스 드라이버 — 하드웨어 통역사
                └─ D5 인터럽트 핸들러 — 벨이 울리면 (1편 IRQ의 나머지 절반)
                    └─ D6 하드웨어 경계 (종착) 🚪→ 1편 「기계 속으로」
```

> v2 확장 후보: 동기화(락·데드락), fork/exec, 파이프·시그널(IPC), inode 깊이,
> 페이지 캐시, 시그널, 컨테이너/네임스페이스.

## 1편과의 다리 (소유권 원칙 이행)
- 가상 메모리: 1편=MMU·TLB ↔ **2편 메모리 관리자=교체 정책·스왑·CoW**
- 인터럽트: 1편=IRQ 신호 ↔ **2편 인터럽트 핸들러=저장·처리·복귀**
- 종착 노드(하드웨어 경계)의 🚪 배지는 1편 **실제 URL**로 연결

## 엔진 재사용
1편에서 복사: index.html / css / app.js (+favicon)
- 테마: 골드 → **바이올렛** (#b08ae0) 교체로 권만의 정체성
- 게이지: 로그 크기축 → **선형 권한축** (GAUGE 설정을 data.js로 분리)
- localStorage 키 분리(os_*) — 같은 도메인에서 1편과 충돌 방지
- 콘텐츠팩 신규 작성: data / scenes / labs / extras / book

## 마일스톤
- M1 ✦ 엔진 이식 + 테마 + 권한 게이지 (이번)
- M2 ✦ 굵은 경로 13노드: 본문·도식·미니랩 1종(strace)·전공노트/퀴즈 (이번)
- M3 배포: repo `operatingSystem` + Pages (이번)
- M4 미니랩 확장 (스케줄러 타임슬라이스, 교체 정책 비교, fork 체험…)
- M5 교재(book) 집필 · 교차링크 확장 · 가지 확장
